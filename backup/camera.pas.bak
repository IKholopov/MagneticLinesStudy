unit Camera;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, GL;

type Camera = class(TObject)
  Rotation:real;
  public
    constructor Create();
   end;
implementation
    constructor Camera.Create();
    begin
         Rotation := 0;
    end;
end.

